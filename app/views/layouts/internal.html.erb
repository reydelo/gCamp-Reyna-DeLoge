<!DOCTYPE html>
<html lang="en">
<head>
  <title>GCamp - Reyna</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body id="top">

  <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">gCamp</a>
      </div>
        <ul class="nav navbar-nav">
          <li class="dropdown" style="padding-left: 15px;">
            <a class="navbar-brand dropdown-toggle" data-toggle="dropdown" type="button" href="#" aria-expanded="true">Projects<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <% current_user.projects.each do |project| %>
              <li><%= link_to project.name, project_path(project) %></li>
              <% end %>
              <li class="divider"></li>
              <li><%= link_to 'New Project', new_project_path %></li>
            </ul>
          </li>
          <li style="padding-left: 15px;"><a class="navbar-brand" href="/users">Users</a></li>
        </ul>

      <div class="nav navbar-nav navbar-right">
        <%= link_to current_user.full_name, user_path(current_user), class: "navbar-brand" %>
        <%= link_to 'Logout', '/logout', class: "navbar-brand" %>
      </div>

    </div>
  </div>

  <% if notice %>
  <div class="container">
    <div class="alert alert-success"><strong><center><%= notice %></center></strong></div>
  </div>
  <% end %>


  <%= yield %>

  <div class="container">
    <footer id="internal">
          <%= link_to "About", about_path %>
          |
          <%= link_to "Terms", terms_path %>
          |
          <%= link_to "FAQ", faq_path %>
      </footer>
    </div> <!-- /container -->

    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="../../dist/js/bootstrap.min.js"></script>
    <!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
    <script src="../../assets/js/ie10-viewport-bug-workaround.js"></script>

  </body>
  </html>
